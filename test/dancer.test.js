describe('Dancer', function () {
  'use strict';

  var assume = require('assume')
    , Dancer = require('./');

  it('is exposed a function', function () {
    assume(Danser).is.a('function');
  });
});
